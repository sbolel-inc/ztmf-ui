name: Orchestration PROD

on:
  pull_request:
    branches:
      - main
    types:
      - closed

jobs:     
  if: ${{ github.event.pull_request.merged }}
  ui:
    name: ui
    uses: ./.github/workflows/ui.yml
    with:
      environment: prod
    secrets: inherit
  